@AGENTS.md

# النعوة الإلكترونية — ذاكرة المشروع والقرارات

هذا الملف يحفظ القرارات التقنية غير البديهية من الكود وحده، لمنع تكرار الأخطاء أو إعادة اكتشاف نفس القيود.

## 1. أساسيات المشروع
- **المنتج**: مولّد نعوات (أوراق نعي) عربية إسلامية بجودة الطباعة — محرر بيانات + معاينة حيّة + تصدير PNG/PDF.
- **التقنيات**: Next.js 16.3.1 (App Router, Turbopack افتراضي)، React 19.2، TypeScript، Tailwind CSS v4، Prisma 7 + Postgres، Zustand، dnd-kit، html-to-image، jsPDF.
- **الواجهة بالكامل عربية RTL** (`dir="rtl"` في `<html>`), لا دعم لغة أخرى.
- الخطة الكاملة والقرارات المعتمدة موثّقة في `C:\Users\TheLight-VEditor\.claude\plans\rtk-iterative-sifakis.md` (خارج المستودع).

## 2. قرارات معمارية غير بديهية

- **محرك القواعد هو قلب المشروع** (`src/lib/obituary/grammar.ts` + `render.ts`) — كل نص في النعوة (تصريف الجنس، تجميع «المرحومين» لسلاسل الوفاة المتتالية، صيغة «زوجته» مقابل «زوجة») يُشتق منه، وليس مكتوباً حرفياً في المكوّنات. أي إضافة فئة قرابة جديدة تبدأ من `defaults.ts` ثم `grammar.ts`.
- **المخطوطات القرآنية تُعرض كنص حيّ بخط عربي حقيقي** (`Amiri Quran` / `Aref Ruqaa Ink` عبر `next/font/google`)، **وليس** كمسارات SVG مولَّدة. جُرِّب توليد المسارات عبر `opentype.js` فعلياً وفشل: إما رمى استثناء GSUB غير مدعوم، أو أنتج حروفاً عربية منفصلة غير موصولة (تحقّق ذلك بصرياً عبر رسترة SVG→PNG). محرك التشكيل الحقيقي في المتصفح (HarfBuzz) هو الحل الصحيح الوحيد؛ **لا تُعِد تجربة opentype.js لهذا الغرض**.
- **`Calligraphy.tsx` يحسم وجود ملف SVG اليدوي عبر `HANDMADE_CALLIGRAPHY_IDS`** (مانيفست ثابت يولّده `scripts/generate-calligraphy-manifest.ts` قبل كل `predev`/`prebuild` بمسح `public/calligraphy/handmade/*.svg`) — **لا** عبر `fetch HEAD` وقت التشغيل (نُقل عمداً من fetch إلى مانيفست ثابت لتفادي فحص شبكي لكل مخطوطة عند كل تركيب). أي `<img src>` مكسور في شجرة `#obituary-canvas` يُفشل `html-to-image` بالكامل عند التصدير (مع `cacheBust: true`) — هذا سبب حقيقي لعطل صادفناه وأُصلح جذرياً بهذا المانيفست.
- **اسم الملف الفعلي (handmadeFile) منفصل عمداً عن معرّف الآية الدلالي (id)** في `QuranVerse` — بعض الآيات (مثال: "وبشّر الصابرين") لها رسمتان مختلفتان لنفس النص (ملفا `2.svg` و`6.svg`)، فكل رسمة معرّف منفصل مع `handmadeFile` يشير للملف الحقيقي. الملفات اليدوية الحالية أسماؤها أرقام (`1,2,3,5,6.svg`) لا نصوصاً دلالية.
- **تصدير PDF يستعمل JPEG بجودة 0.92 لا PNG خام** — `jsPDF.addImage` مع PNG خام أنتج ملفات ~24 ميجابايت غير عملية للمشاركة؛ JPEG بنفس الدقة (`pixelRatio: 3`) أنتج ~570 كيلوبايت بلا فرق بصري ملحوظ على النص. تصدير PNG المباشر (للتحميل والمشاركة) يبقى بلا فقدان.
- **Auto-fit** (`useAutoFit.ts`) يغيّر `fontSize` الجذر عبر بحث ثنائي حتى يبقى المحتوى داخل ٧٩٤×١١٢٣px (A4 عند 96dpi) — ضروري لأن كثافة الأقارب تتراوح فعلياً بين ~١٢ و~٣٠ اسماً بين نعوة وأخرى.
- **٧ قوالب لا ٦** — أُضيف "الليلي الفخم" (`midnight-elegant`): خلفية سوداء + إطار ذهبي، **يُعيد استعمال `RoyalFrame` نفسه** (الإطارات مبنية على `--tpl-accent`/`--tpl-ink` فقط، فتعمل بصرياً على أي خلفية بلا أي كود جديد). طُلب هذا صراحةً كبديل عن فكرة أولى مرفوضة (تصدير بإطار أسود محيط بالبطاقة نفسها) — **لا تُعِد إضافة خيار "تصدير بخلفية سوداء" في ExportBar**، القالب الداكن هو الحل المعتمد.
- **القوالب الستة تختلف بنيوياً** (إطار SVG + خط + تخطيط اسم + كثافة زخرفة عبر `--tpl-*` CSS vars)، لا لوناً فقط — راجع `src/lib/templates/registry.ts`.
- **`ResponsiveCanvasFrame.tsx` يوسّط الكانفاس (٧٩٤px) داخل حاوية أضيق بـ flexbox (`flex justify-center`) لا بـ `margin: auto`.** هذا عطل حقيقي صادفناه: `margin: auto` **لا يوسّط عنصراً أعرض من حاويته** — المتصفح يحسم الهامش التلقائي إلى صفر بدل توزيعه (قاعدة CSS قياسية)، فيلتصق الصندوق بحافة البداية المنطقية (اليمين في هذا التطبيق الـRTL) ويفيض بالكامل يساراً خارج نطاق الرؤية. ظهر هذا كنعوة شبه فارغة على الجوال (لقطة شاشة أظهرت اسم الفقيد مقصوصاً لحرف واحد). الفلكس وحده يوسّط عنصراً أكبر من حاويته بتماثل صحيح.
- **مقاس الصفحة مصدر وحيد**: `src/lib/obituary/pageSize.ts` يصدّر `A4_WIDTH_PX`/`A4_HEIGHT_PX` (٧٩٤×١١٢٣) — يستعملها `ObituaryCanvas.tsx`، `ResponsiveCanvasFrame.tsx`، و`ObituaryBlocks.tsx` معاً. **لا تُضِف قياساً مختلفاً أو خياراً لتغيير الحجم** — طُلب صراحةً أن يبقى A4 حصراً بلا تكبير.
- **الفقرات الثلاث الأخيرة (الراضون بقضاء الله + إنّا لله وإنّا إليه راجعون + خاتمة الدعاء) مثبّتة أسفل الصفحة دائماً** عبر `marginTop: "auto"` على غلافها، معتمدةً على `minHeight: A4_HEIGHT_PX` (رقم px ثابت غير متأثر بمقياس em) على جذر `ObituaryContent` في `ObituaryBlocks.tsx`. **هذا العدد الثابت لا يفسد حساب auto-fit** — `min-height` بوحدة px مطلقة لا تتأثر بتغيّر `fontSize` الذي يضبطه `useAutoFit`، فتبقى مقارنة `scrollHeight` صحيحة (تتجاوز 1123 فقط إن فاض المحتوى فعلياً، بصرف النظر عن الحد الأدنى). لا تُبدِّل هذه الآلية بـ `flex-grow` على عنصر آخر — يكسر القياس.
- **فئة "الوالدين" (`parents`) موحّدة في المحرر لكنها تُطبع كسطرين منفصلين** ("والده:"/"والدته:") — `createEmptyRelativeGroup("parents")` تُنشئها بعضوين جاهزين (ذكر/أنثى) دائماً، و`relativesBlocks` في `render.ts` تفصلهما بجنس العضو نفسه (لا فهرس)، مستعملة `PARENT_LABELS` في `grammar.ts`. لا تُعِد فصلهما إلى فئتين منفصلتين في `RelativeCategoryKey`.
- **`FIXED_GENDER_BY_CATEGORY` في `defaults.ts`** يحدّد أي فئات قرابة جنسها ثابت (بناته=أنثى، أولاده=ذكر...) فيُخفى محدِّد الجنس في `Step3Relatives.tsx` لها — الفئات المختلطة فعلياً (`parents`, `grandchildren`, `cousins_*`, `nephews_*`) تبقيه ظاهراً. أي فئة جديدة أحادية الجنس يجب إضافتها هنا لتجنّب تكرار عطل "لماذا يظهر ذكر/أنثى في بناته؟".
- **مقاسات المخطوطات مشتقة من سنتيمترات فعلية لا من تخمين بصري**: `CM_TO_PX = 794/21` (عرض A4 = ٧٩٤px = ٢١سم عند 96dpi) في `defaults.ts` يُنتج `CALLIGRAPHY_DEFAULT_WIDTH_PX` (~15سم لمعظم المخطوطات) و`BASMALA_WIDTH_PX` (~10سم) و`targetWidthPx` الخاص بآية الفجر ٢٧-٣٠ (~18سم، لأنها الرسمة الوحيدة العريضة/أفقية). **السبب الجذري لعطل "المخطوطات كبيرة جداً" السابق**: ملفات SVG اليدوية لا تحمل `width`/`height` صريحين (فقط `viewBox`)، فيرسمها المتصفح بأبعاد الـviewBox الفعلية (مئات إلى آلاف البكسل) ما لم يُفرض `width` صراحةً — لذا `widthPx` بات **prop إلزامياً** على `Calligraphy.tsx`، لا اختيارياً.
- **صورة الفقيد تتقلّص مع auto-fit لكن بحد أقصى ٣٠٪** — `useAutoFit.ts` يعيد `scale` (كان أُزيل كـ"غير مستخدم" في مرحلة `/simplify` سابقة ثم أُعيد لوجود مستهلك شرعي جديد)، و`ObituaryBlocks.tsx` يحسب `photoScale = Math.max(scale, 0.7)` بدل استعمال `scale` مباشرة — طُلب صراحة ألا تصغر الصورة بقدر ما يصغر النص عند كثافة الأسماء العالية.
- **القالب الافتراضي عند إنشاء نعوة جديدة هو `modern-minimal` ("الزجاجي الحديث")** — `createEmptyData()` في `editorStore.ts`. لا تُعِده إلى `gold-classic` أو أي قالب آخر بلا طلب صريح.
- **إضافة فئة قرابة تتفعّل فوراً عند الاختيار من القائمة المنسدلة، بلا زر منفصل** — `Step3Relatives.tsx` يبقي الـ`<select>` **بلا حالة محلية** (`value=""` دائماً، مع خيار أول فارغ كنص توضيحي)، و`onChange` يستدعي `addRelativeGroup` مباشرة ثم يعيد الـselect لقيمته الفارغة تلقائياً (لأن القيمة المختارة تُحذف من قائمة `available` بعد الإضافة). لا تُعِد زر "إضافة فئة قرابة" المنفصل — طُلب صراحة إزالة خطوة النقر الإضافية.
- **علامتا "البسملة" و"إنّا لله" (hasBasmala/hasInnaLillah) لهما مقاس مستقل أصغر بكثير من مقاس المخطوطة القرآنية الرئيسية** — `BASMALA_WIDTH_PX` صار ~٣سم (كان ١٠سم)، و`INNA_LILLAH_FOOTER_WIDTH_PX` (~٤.٥سم) ثابت **جديد ومنفصل** في `defaults.ts` يُستعمل حصراً في `ObituaryBlocks.tsx` لعلامة الختام أسفل الصفحة. **لا تُغيّر `targetWidthPx` على مُدخلة `"inna-lillah"` في `QURAN_VERSES`** لتصغيرها — نفس المُدخلة تُستعمل أيضاً كخيار في قائمة "المخطوطة القرآنية" الرئيسية أعلى الصفحة (quranVerseId)، وتغيير `targetWidthPx` هناك يُصغّرها خطأً في ذلك السياق المختلف كلياً (مخطوطة رئيسية مقابل علامة ختام صغيرة).
- **قالب "الليلي الفخم" (خلفية سوداء) يحوّل كل مخطوطات SVG اليدوية إلى الأبيض عبر `TemplateDefinition.calligraphyInvert: true`** — يُطبَّق كفلتر CSS `brightness(0) invert(1)` على عنصر `<img>` في `Calligraphy.tsx` (وليس `invert(1)` وحدها) لأن `brightness(0)` يوحّد أي تفاوت في درجة حبر المخطوطة الممسوحة ضوئياً إلى أسود خالص أولاً، فيضمن قلباً نظيفاً لأبيض خالص بدل درجات رمادية متبقية. أي قالب داكن جديد يحتاج نفس المعالجة يجب أن يُفعّل هذا الحقل، لا أن يُعيد كتابة الفلتر يدوياً في مكان آخر.
- **`QuranVerse.liveTextFontSizeEm` يتحكّم بحجم النص الحيّ عند غياب مخطوطة SVG يدوية** — `widthPx`/`targetWidthPx` **لا** يكبّران النص الحيّ فعلياً (يقيّدان عرض اللف فقط)؛ الحجم البصري الحقيقي للنص يحتاج `font-size` صريحاً. آية "نفس مطمئنة" (`nafs-mutmainna`) هي الوحيدة بلا ملف SVG يدوي، فحصلت على `liveTextFontSizeEm: 2.5` (تكبير ١٥٠٪ عن ١em الافتراضي) لتضاهي بصرياً حضور المخطوطات المرسومة الأخرى. أي آية جديدة بلا رسمة يدوية تحتاج تكبيراً مماثلاً يجب أن تستعمل هذا الحقل، لا `widthPx` وحده.
- **حجم البسملة (`BASMALA_WIDTH_PX`) مُقاس بالسنتيمتر الفعلي عبر `CM_TO_PX`**، مثل باقي المخطوطات — عُدِّل مراراً بناءً على تغذية راجعة بصرية مباشرة من المستخدم (١٠سم → ٣سم → ٧.٥سم → ٦سم حالياً)؛ لا تفترض رقماً "نهائياً" لهذا الثابت، توقّع تعديلات أخرى مستقبلاً.
- **حقل "بلد الوفاة" (`deceased.country`) مخفيّ من واجهة (بيانات الفقيد) بطلب صريح، لكنه لا يزال موجوداً في نموذج البيانات** — يُستعمل فقط للإحصاءات المجهولة الهوية (`ObituaryStat`)، بقيمته الافتراضية من `createEmptyData()` ("لبنان") بلا أي واجهة تعديل. لا تحذف الحقل نفسه من `types.ts`/`defaults.ts` — فقط أُخفي عنصر النموذج في `Step1Deceased.tsx`.
- **عبارة "المرحوم" فوق اسم الفقيد قابلة للتخصيص عبر `deceased.marhoomStyle` (`"marhoom" | "shaheed" | "custom"`) + `marhoomCustomText`** — يحسمها `marhoomWord()` في `render.ts` عبر جدول `MARHOOM_STYLE_WORDS` الجديد في `grammar.ts` (بديل عن `DECEASED_WORDS.marhoom` المباشر، الذي بقي فقط كافتراضي fallback). **هذا منفصل تماماً عن "المرحوم/المرحومين" المستعملة لتجميع الأقارب المتوفّين في `renderRelativeList`** (grammar.ts) — لا تخلط الاثنين؛ تغيير أحدهما لا يجب أن يمسّ الآخر.
- **"الجهة الناعية" (institutionHeader) عريضة افتراضياً (`funeral.institutionHeaderBold ?? true`)، قابلة للإلغاء من (٢. الجنازة والتعزية)**، وحجمها ثابت الآن عند `1.15em` (بدل `1em`) — تكبير دائم غير قابل للتعديل من المستخدم، منفصل عن خيار bold القابل للتبديل.
- **"ويوارى الثرى في {مكان الدفن}" ملحقة داخل `funeralSentence()` نفسها (بفاصلة عربية «،»)، وليست `<p>` منفصلة في `ObituaryBlocks.tsx`** — كانت تظهر كسطر/فقرة مستقلة تحت جملة الصلاة فلا تبدو استمراراً بصرياً لها؛ الدمج داخل النص المُركَّب في `render.ts` (لا في المكوّن) يتّبع مبدأ "محرك القواعد هو مصدر كل نص" أعلاه. الفاصلة تظهر فقط إن وُجد `funeral.burialLocation` فعلياً (بلا فاصلة معلَّقة إن غاب).
- **تسميات فئات القرابة (زوجاته، أولاده، بناته، أعمامه…) تتحوّل تلقائياً لصيغة المفرد حين تحوي المجموعة عضواً واحداً فقط** — `relativeCategoryLabel()` في `grammar.ts` أصبحت تقبل `members: Person[]` (بارامتر ثالث اختياري، افتراضياً `[]` حفاظاً على توافق الاستدعاءات القديمة بحجّتين فقط)، وتراجع جدولي `SINGULAR_LABELS_FIXED`/`SINGULAR_LABELS_MIXED` الجديدين حين `members.length === 1`. **الفئات ثابتة الجنس** (`wives`, `sons`, `daughters`, `brothers`, `sisters`, `uncles_*`, `aunts_*`, `inlaws_*`) تحسم المفرد بجنس الفقيد فقط. **الفئات المختلطة** (`grandchildren`, `cousins_*`, `nephews_*`) تحتاج **أيضاً** جنس العضو الوحيد نفسه (حفيد ذكر ← "حفيده"، حفيدة أنثى ← "حفيدته" — نفس الفقيد الذكر، نتيجة مختلفة). أي فئة جديدة تحتاج هذا السلوك يجب أن تُضاف لأحد الجدولين حسب كونها ثابتة أو مختلطة الجنس. فئة "الوالدين" مستثناة عمداً (مُعالَجة بمسار منفصل كلياً في `relativesBlocks`).
- **`createEmptyData()` تبدأ بـ `relatives: []` — بلا فئة "الوالدين" مُفعَّلة سلفاً** (كانت مُفعَّلة بالتصميم القديم؛ أُزيلت بطلب صريح بعد أن صار إضافة فئة قرابة فورياً بلا زر منفصل، فلا حاجة لتلك الفئة الجاهزة أصلاً). **هذا أصلح أيضاً عطلاً حقيقياً في ترتيب فئات القرابة**: `data.relatives[]` ترتيبها append-only بالكامل (`addRelativeGroup` يضيف دائماً للنهاية، `relativesBlocks`/`Step3Relatives.tsx` يعرضان بترتيب المصفوفة كما هو بلا أي فرز) — فوجود "الوالدين" ثابتة عند الفهرس ٠ دائماً كان يكسر مطابقة "ترتيب الإضافة = ترتيب العرض" حين يضيف المستخدم فئة أخرى أولاً ثم يملأ "الوالدين" لاحقاً. لا تُعِد أي فئة قرابة افتراضية مُفعَّلة سلفاً بلا طلب صريح — هذا يكسر نفس الافتراض مجدداً.
- **حقل "تاريخ الصلاة والدفن" (`funeral.burialDateISO`) بلا قيمة افتراضية في `createEmptyData()`** (لم يعد `todayISO()`) — يُملأ بتاريخ اليوم فقط عند أول `onFocus` على حقل الإدخال في `Step2Funeral.tsx` (فقط إن كان فارغاً فعلاً)، لا تلقائياً عند تحميل الصفحة. `funeralSentence()` في `render.ts` تتراجع تلقائياً إلى `deceased.deathDateISO` حين يبقى هذا الحقل فارغاً (`data.funeral.burialDateISO || data.deceased.deathDateISO`)، فالسلوك سليم بلا هذه القيمة الافتراضية.
- **حقل "تاريخ الوفاة" (`deceased.deathDateISO`) بلا قيمة افتراضية أيضاً — نفس نمط `onFocus`** في `Step1Deceased.tsx` (يُملأ بتاريخ اليوم فقط عند أول تركيز، إن كان فارغاً). النوع في `types.ts` بقي `string` غير اختياري عمداً (لا `string | undefined`) — القيمة الفارغة `""` تكفي، و`formatDualDate`/`formatWeekdayName` في `hijri.ts` يتعاملان مع سلسلة فارغة بأمان (يُعيدان `""` بلا رمي استثناء)، فبقاء الحقلين فارغين معاً لا يكسر `funeralSentence()`.
- **صفحة `/create` تعرض الآن `CreateHeader.tsx`** (شعار `public/icon.png` + اسم المشروع عبر `BrandLogo.tsx` المشترك + أزرار مختصرة: PNG/PDF/مشاركة/إعدادات النصوص) — **نطاقها هذا الشريط فقط، وليست بداية تعميم الوضع المظلم على بقية المحرر** (القرار المتعمَّد: تقييد التغيير لِـ"hero بسيط" كما طُلب حرفياً؛ بقية عناصر `/create` — البطاقات، الحقول، الكانفاس — تبقى فاتحة اللون دائماً بغضّ النظر عن حالة المفتاح). إن طُلب تعميم الوضع المظلم لاحقاً على كل `/create`، فذلك يتطلب مهمة منفصلة أكبر (استبدال الألوان الثابتة الكثيرة عبر `ui/Button.tsx`/`Card.tsx`/`Field.tsx` وخطوات المحرر بمتغيّرات CSS مواضيعية).
- **"التعزية العامة" (`funeral.condolencesGeneral`) تُملأ فعلياً (لا placeholder رمادي فقط) عند أول `onFocus`** — نفس نمط حقول التاريخ (تاريخ الوفاة/تاريخ الصلاة والدفن): فارغة افتراضياً، والنقر عليها يضع النص الجاهز كقيمة حقيقية قابلة للتعديل مباشرة. **ملاحظة**: نص `DEFAULT_CONDOLENCES_GENERAL` المحلي في `Step2Funeral.tsx` تغيّر لاحقاً (خارج هذا التعديل) من "جبانة الثرى" الحرفية إلى "(إسم المكان)" كقالب عام أذكى (لا يفترض مكاناً بعينه لكل مستخدم) — إن أردت النص الحرفي الأصلي بدل القالب العام، عدّله يدوياً.
- **`BrandLogo.tsx` (شعار + اسم المشروع، مشترك بين الترويستين) يتصرّف حسب الصفحة الحالية** — `Link href="/"` دائماً، **إلا** إن كنّا على `/` أصلاً (`usePathname() === "/"`)، فعندها `preventDefault` + `window.scrollTo(top:0)` بدل تنقّل بلا أثر (Next.js Link إلى المسار الحالي نفسه لا يُنجز شيئاً افتراضياً). من `/create` تحديداً: النقر يأخذ إلى الصفحة الرئيسية فعلياً — هذا هو السلوك المطلوب صراحةً هناك.
- **زر تبديل الوضع (`ThemeToggle`) أُزيل من `CreateHeader.tsx` بطلب صريح** — الترويسة تبقي `.home-scope` (لتوحيد لغة الألوان بصرياً) لكن بلا زر تحكّم ظاهر فيها؛ إن بُدِّل الوضع من الصفحة الرئيسية، ينعكس تلقائياً هنا أيضاً عبر نفس مفتاح `localStorage` المشترك (`THEME_INIT_SCRIPT` في `/create` يقرأه عند التحميل). لا تُعِد إضافة الزر هنا بلا طلب صريح جديد.
- **آلية الوضع المظلم أصبحت مشتركة بين `/` و`/create` عبر نفس المفتاح**: `ThemeToggle.tsx` انتقل من `components/landing/` إلى `components/theme/` (مكان محايد، لم يعد حصرياً على الصفحة الرئيسية؛ لا يزال يُستهلَك من `LandingHeader.tsx` فقط حالياً بعد إزالته من `CreateHeader.tsx`)، ونص تهيئة السمة قبل أول رسم (لمنع الوميض) استُخرج إلى `lib/theme/initScript.ts` (`THEME_INIT_SCRIPT`) ليُستهلَك من كلا الصفحتين بلا تكرار حرفي للسكربت. **الصنف `.home-scope` نفسه (وأسماء متغيّراته `--home-*`) أُعيد استعماله حرفياً** في `CreateHeader.tsx` بدل تعريف نسخة مكرّرة باسم مختلف — أي عنصر جديد يريد التفاعل مع هذا المفتاح يجب أن يكون هو نفسه (أو أحد أسلافه) داخل `.home-scope`، وإلا سيبقى بمظهره الافتراضي بلا أي أثر.
- **منطق التصدير (التقاط الكانفاس + PNG/PDF/مشاركة) استُخرج إلى `lib/export/actions.ts` كدوال مستقلة** (`exportPng`/`exportPdf`/`exportShare`) — يستهلكها كل من `ExportBar.tsx` (الشريط الكامل بتسميات نصية أسفل المعاينة) و`CreateHeader.tsx` (أزرار أيقونات مختصرة أعلى الصفحة)، كل بحالة `busy`/`error` محلية خاصة به. أي تعديل على منطق التصدير نفسه (لا التصميم) يجب أن يبقى في هذا الملف المشترك وحده.
- **`ExportSettingsPanel.tsx` أُعيدت تسميته إلى `TextSettingsPanel.tsx`** (وسّع نطاقه من "خط اسم الفقيد فقط" إلى "إعدادات النصوص" عامةً — راجع البند التالي) — يقبل `placement` (`"up"` الافتراضي/`"down"`) و`triggerClassName`. `CreateHeader.tsx` يستعمل `placement="down"` (اللوحة تُفتح أسفل الترويسة العلوية بدل أعلاها) و`triggerClassName` مخصّص (بدل `Button` المشترك ذي الألوان الثابتة) لمطابقة أزرار الترويسة الأخرى بصرياً. اللوحة تحمل **`left-0` إلزامياً** على حاويتها — بلا هذه المرساة الأفقية الصريحة كانت تفيض فعلياً خارج الحافة اليسرى للصفحة حين يكون الزر المُشغِّل قريباً منها (عطل حقيقي صادفناه تحديداً مع زر الترويسة العلوية القريب من تلك الحافة؛ زر ExportBar أسفل المعاينة لم يُظهر العطل لبعده عنها، لكن الإصلاح غير مشروط بالموضع عمداً). **محتوى اللوحة نفسها (الخلفية/الحقول) بقي بمظهره الفاتح الثابت في كل الحالات عمداً** — قرار تبسيط: نافذة منبثقة فاتحة فوق ترويسة قابلة للتبديل هو نمط شائع غير مكسور.
- **"إعدادات النصوص" (`TextSettingsPanel.tsx`) صارت تحتوي قسمين**: (١) "خط جميع النصوص" — `bodyFontFamily` جديد على `ObituaryData`، يُطبَّق على جذر `ObituaryContent` في `ObituaryBlocks.tsx` (`fontFamily: data.bodyFontFamily || tokens.bodyFont`)، و(٢) "إعدادات اسم الفقيد" (كانت كل محتوى اللوحة سابقاً، الآن قسم فرعي تحتها). **هذا أصلح عطلاً كامناً حقيقياً**: `tokens.bodyFont` كان مُعرَّفاً لكل قالب في `templates/registry.ts` منذ البداية لكنه **لم يُطبَّق على أي عنصر إطلاقاً** — كل فقرات النعوة (جملة النعي، الأقارب، صلاة الجنازة، التعزية…) كانت تُعرض دائماً بخط الصفحة الافتراضي (Cairo، من `body{font-family}` في globals.css) بصرف النظر عن `bodyFont` المقصود لكل قالب (فقط اسم الفقيد والمخطوطة القرآنية كانا يطبَّقان فعلياً عبر مسارات مستقلة). القوالب الآن تبدو أكثر تمايزاً فعلياً بعد هذا الإصلاح.
- **كتالوج الخطوط أُعيد تسميته من `lib/nameFonts.ts` (`NAME_FONT_OPTIONS`) إلى `lib/textFonts.ts` (`TEXT_FONT_OPTIONS`)** — يخدم كلا القسمين في `TextSettingsPanel.tsx` معاً (خط عام + خط الاسم)، بدل أن يكون مخصَّصاً للاسم فقط. أُضيفت ٤ خطوط جديدة عبر `next/font/google` في `layout.tsx` بعد التحقّق من وجودها فعلياً في ملف تعريف الأنواع المُصرَّح محلياً (`node_modules/next/dist/compiled/@next/font/dist/google/index.d.ts`) ودعمها الرسمي لـ`subsets: ["arabic"]` قبل إضافتها — **لا تفترض دعم subset عربي لخط جديد بلا هذا التحقّق المباشر أولاً**: `Alexandria`، `IBM_Plex_Sans_Arabic`، `Noto_Naskh_Arabic`، و`Reem_Kufi` (العائلة العادية القابلة للقراءة — **منفصلة عمداً عن `Reem_Kufi_Ink`** الزخرفي الموجود سابقاً والمُستعمل حصراً في `calligraphyFont` لقالب `royal-monogram`؛ متغيّرا CSS مختلفان: `--font-reem-kufi-plain` مقابل `--font-reem-kufi`، **لا تُعِد استعمال نفس اسم المتغيّر لكليهما**). أُضيف أيضاً "أميري قرآن" (`--font-amiri-quran`، مستورد أصلاً لكن لم يكن مُتاحاً كخيار نص عام) كخيار مستقل في الكتالوج.

## 3. قيود Next.js 16 (تخالف تدريب أي نموذج على Next 14/15)

- Turbopack افتراضي في `dev` و`build` — لا حاجة لـ `--turbopack`.
- `headers()`/`cookies()`/`params`/`searchParams` غير متزامنة إجبارياً.
- `next lint` محذوف — استُبدل بـ `npx eslint src scripts` مباشرة.
- **`tsconfig.json`'s `exclude` يجب أن يبقى شاملاً `agent-skills` و`.agents`** — هذان مجلدان لأدوات خارجية مُثبَّتة بجانب المشروع، لهما `package.json` وتبعيات خاصة بهما غير مثبَّتة هنا. حذف هذا الاستثناء يُغرق `tsc`/`vitest` بمئات الأخطاء غير ذات الصلة (تحقّق دائماً بتشغيل `npx tsc --noEmit` بعد أي تعديل على `tsconfig.json`).
- كذلك `vitest.config.ts` يحصر الاختبارات بـ `test.include: ["src/**/*.test.ts"]` للسبب نفسه.
- **`globals.css` يستثني `agent-skills`/`.agents` من فحص Tailwind v4 التلقائي** عبر `@source not "../../agent-skills"` وما شابه. بدونه، Tailwind يمسح هذين المجلدين ويلتقط نصوصاً عشوائية تشبه أصناف CSS فيولّد CSS غير صالح — كان تحذيراً صامتاً في `next build` لكنه يُسقط الصفحة بالكامل (شاشة بيضاء) في `next dev`.
- **تحديد `overflow-y` وحده على عنصر يجعل المتصفح يحوّل `overflow-x` تلقائياً إلى `auto`** (قاعدة CSS قياسية: لا يبقى أحدهما `visible` بينما الآخر ليس كذلك). أي حاوية فيها `overflow-y-auto` فقط يجب أن تحمل `overflow-x-hidden` (أو `visible`) صراحةً بجانبها، وإلا نتج قصّ أفقي غير مقصود.

## 4. Prisma
- المخرج مخصص: `output = "../generated/prisma"` في `schema.prisma` — الاستيراد نسبي: `from '../../generated/prisma'` (من `src/lib/`) أو `from '../generated/prisma'` (من `prisma/`)، **وليس** `@prisma/client`.
- الاتصال عبر `PrismaPg` adapter + `pg.Pool`، والـ `url` في `prisma.config.ts` وليس `datasource` block.
- **`prisma migrate dev` يرفض العمل في بيئة غير تفاعلية بالكامل** (حتى مع `--create-only`). البديل: `prisma db push --accept-data-loss`.
- **Prisma نفسه يكتشف استدعاء Claude Code ويرفض أي أمر مدمّر** (`db push --accept-data-loss`, إلخ) إلا بتعيين `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION` = نص موافقة المستخدم الصريحة الفعلي، بعد الإفصاح الكامل له عن الأمر وأثره. لا تحاول تجاوز هذا التحقق.
- النموذجان الحاليان: `ObituaryStat` (إحصاءات مجهولة الهوية، `ipHash` مُجزَّأ بـ `IP_SALT` من `.env`) و`Template`. **لا** نموذجي `User`/`Post` القديمين (كانا من مشروع سابق واستُبدلا).

## 5. الأوامر الشائعة

```bash
npm run dev                          # Turbopack افتراضي
npm run build && npm run start       # تحقّق إنتاجي فعلي قبل أي تسليم
npx tsc --noEmit --project tsconfig.json
npx eslint src scripts
npm run test                         # vitest — يغطي محرك القواعد أساساً
npx prisma db push --accept-data-loss   # لا migrate dev — انظر القسم ٤
npx tsx prisma/seed.ts               # زرع القوالب الستة
npx tsx scripts/verify-prisma.ts     # تحقّق الاتصال
```

## 6. تنبيهات
- لا تستخدم `import` داخل سطر الأوامر (Terminal/PowerShell).
- حافظ على `.env` داخل `.gitignore` ولا تطبع محتواه أبداً.
- عند تعديل `src/lib/obituary/grammar.ts` أو `render.ts`: شغّل `npm run test` فوراً — الاختبارات مبنية على نصوص حرفية من نعوات حقيقية (راجع تعليقات `grammar.test.ts`)، وأي كسر فيها يعني نصاً خاطئاً فعلياً في الإنتاج.
